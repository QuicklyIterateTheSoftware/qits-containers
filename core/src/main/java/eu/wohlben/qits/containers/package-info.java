/**
 * The qits-containers domain: the registry rows, the workload spec and its lifecycle policies, and
 * the docker seam the service module implements.
 *
 * <p>Empty in the scaffold commit. The package exists so the root of the tree is claimed by the
 * module that owns the datasource, the persistence unit and the Flyway lineage — {@code
 * eu.wohlben.qits.containers.client} is another module's and depends on nothing here.
 */
package eu.wohlben.qits.containers;
