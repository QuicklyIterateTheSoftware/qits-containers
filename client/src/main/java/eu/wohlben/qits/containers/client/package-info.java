/**
 * The consumer-facing client for qits-containers: the wire records and the HttpClient behind them.
 *
 * <p>Empty in the scaffold commit, and it depends on {@code qits-containers-core} not at all. That
 * is the module's whole reason to exist: a caller wants a few records and a client, while the
 * domain jar carries entities, a Flyway lineage and datasource defaults that would arrive in every
 * consumer with it.
 */
package eu.wohlben.qits.containers.client;
